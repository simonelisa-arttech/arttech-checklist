# P3.3 — Onboarding cliente: set-password link + config

> **Fonte roadmap:** `docs/ASSISTENZA_FLUSSO_LEDCARE.md` §6.1 / §7 (P3.3).
> **Stato:** codice pronto (PR dedicata) · **Config Supabase/Resend:** da applicare a cura del CEO (valori esatti sotto).
> **Creato:** 2026-07-02.

## 1. Cosa cambia (codice)

Il flusso di auto-registrazione area cliente **esisteva già** (`/api/public/portal-register`): match email in `clienti_anagrafica` → crea utente Supabase Auth + record `clienti_portale_auth` → invia email. P3.3 **migliora** il caso "match univoco senza accesso":

| Prima | Dopo (P3.3) |
|-------|-------------|
| `createUser` con **password temporanea** generata + email con password **in chiaro** | `createUser` **senza password** + email con **link set-password** (recovery), stesso primitivo del reset operatori |
| Cliente accede con password temporanea e "dovrebbe" cambiarla | Cliente imposta lui la password dal link, poi atterra **direttamente su `/cliente`** |
| `/registrazione` non precompila l'email | `/registrazione?email=<x>` **precompila** l'email (deep-link dalla landing LedCare) |

Flusso completo: `/registrazione?email=…` → POST `/api/public/portal-register` → (match univoco) crea utente + `clienti_portale_auth` + `generateLink(recovery, redirectTo=/auth/callback)` → **email con link** → click → `/auth/callback` (`verifyOtp` recovery) → `/reset-password` (imposta password) → **redirect ruolo-aware: CLIENTE → `/cliente`** (altri → `/login`).

Sicurezza: nessuna password in chiaro via email; il link è personale e a scadenza (gestito da Supabase). I casi "email non in anagrafica / ambigua / accesso disattivato / accesso già attivo" restano invariati (richiesta pending + notifica staff / messaggio "exists").

### File modificati
- `app/api/public/portal-register/route.ts` — set-password link invece di password temporanea (nuovi helper `generatePortalSetupLink`, `sendSetupPasswordEmail`; rimossa `generateTemporaryPassword`).
- `app/registrazione/page.tsx` — prefill `?email=` (wrap in `<Suspense>` per `useSearchParams`).
- `app/reset-password/page.tsx` — dopo `updateUser`, redirect `CLIENTE → /cliente` (fallback `/login`).
- `tests/registrazione-prefill.spec.ts` — E2E prefill email (nuovo).

### Migration DB
**NESSUNA.** Le tabelle `clienti_portale_auth` e `portal_registration_requests` esistono già; il flusso non aggiunge colonne/tabelle.

## 2. Config Supabase Auth (da applicare)

Dashboard Supabase → progetto **`aaiuyaiwdrecyqjgnjxp`** → **Authentication → URL Configuration**.

- **Site URL:** `https://atsystem.arttechworld.com`
- **Redirect URLs (allow list)** — devono includere (una per riga):
  - `https://atsystem.arttechworld.com/auth/callback`
  - `https://atsystem.arttechworld.com/reset-password`
  - `http://localhost:3000/auth/callback`  *(dev locale)*
  - `https://*.vercel.app/auth/callback`  *(preview Vercel; se il wildcard non è accettato, aggiungere il dominio preview specifico)*

**Note importanti:**
- L'unico URL **validato da Supabase** è il `redirectTo` del link = `…/auth/callback`. Se non è in allow-list, Supabase ripiega sul Site URL e il flusso si rompe.
- `/cliente` **NON** va in allow-list: è un redirect **client-side** (Next router) fatto da `/reset-password` dopo il set-password, non un redirect validato da Supabase.
- Questi URL sono **già usati** dal reset password operatori (`/api/auth/password-recovery` → `redirectTo=/auth/callback`): se il reset operatori funziona in prod, l'allow-list prod è già a posto → **verificare solo** che ci siano anche i domini **preview** e **localhost**.

## 3. Env vars (verificare presenza su prod + preview)

Già usate dal codice esistente — nessuna nuova variabile introdotta da P3.3:
- `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- `NEXT_PUBLIC_SITE_URL=https://atsystem.arttechworld.com` (o `NEXT_PUBLIC_APP_URL`; default hardcoded al dominio prod)
- `RESEND_API_KEY`, `EMAIL_FROM` (mittente su **dominio verificato in Resend**)
- Opzionali: `REGISTRATION_NOTIFY_EMAIL` (notifiche staff), `ALLOWED_ORIGINS` (CORS extra per il form pubblico)

**Resend vs SMTP Supabase:** l'email con il link parte dal **nostro Resend** (`lib/email.ts`), non dall'SMTP di Supabase. Quindi **non** serve configurare l'SMTP in Supabase; serve solo che `EMAIL_FROM` sia su dominio verificato in Resend. (Con `E2E=1` l'invio è mockato.)

## 4. Test

- **CI/E2E automatico:** aggiunto `tests/registrazione-prefill.spec.ts` (prefill `?email=`), gira nel check "e2e" della PR. Le email sono mockate (`E2E=1`).
- **Non automatizzabile in CI:** il flusso completo link → set-password → `/cliente` richiede una mailbox reale → **test manuale in dev/preview** (sotto).

### Test manuale (dev o preview)
1. `npm run dev` (con `RESEND_API_KEY`/`EMAIL_FROM` reali per ricevere l'email; senza `E2E=1`).
2. In `clienti_anagrafica` scegliere un'email **attiva, univoca, senza** record `clienti_portale_auth`.
3. Aprire `/registrazione?email=<quella-email>` → verificare che l'email sia **precompilata** → compilare ragione sociale → **Registrati** (atteso: messaggio "ti abbiamo inviato un'email con il link…").
4. Aprire l'email → cliccare **"Imposta la password e accedi"** → `/auth/callback` → `/reset-password` → impostare la password → atteso: atterraggio su **`/cliente`** già loggato.
5. Verificare in DB: 1 riga in `clienti_portale_auth` (user_id ↔ cliente_id) e 1 riga `auto_approved` in `portal_registration_requests`.

## 5. Rollback

Revert della PR. Nessuna operazione distruttiva: non tocca schema né dati. Gli account già creati (anche quelli con vecchia password temporanea) restano validi; cambia solo il **metodo di consegna** dell'accesso ai nuovi.
